# CodeLocal — Plugin Listing Draft

## Public name

**CodeLocal**

## One-line positioning

**A universal MCP layer with one durable Project Brain and controlled local execution for AI coding.**

## Short description

Bring CodeLocal's universal MCP Project Brain to ChatGPT and Codex: keep durable project context across conversations while repository access, Git, terminal and local tools remain behind explicit workspace permissions and approval controls.

## Long description

CodeLocal is a universal MCP connection layer for AI coding. This OpenAI Plugin brings that same CodeLocal Project Brain and controlled runtime to ChatGPT and Codex, while other MCP-compatible clients can connect to the same CodeLocal endpoint. The assistant can understand an authorized codebase, navigate symbols, edit files, work with Git, run guarded commands, verify changes and reuse verified project knowledge across later conversations without rebuilding that project context for every compatible AI client.

Project Brain preserves useful rules, decisions, project facts, verified Experience and reusable workflows without treating every operational event as long-term memory. CodeLocal compiles a bounded task-specific context packet instead of blindly replaying the entire project history.

Local execution remains permission-aware. Raw source code, project secrets, local credentials and terminal execution context are intended to stay on the paired machine. Risky actions remain subject to CodeLocal policy and user approval. Semantic retrieval is optional and falls back to deterministic behavior when its readiness or quality gates are not met.

Use CodeLocal when you want an AI coding assistant to understand a long-lived project, make reviewable changes, prove those changes with local verification, and retain the useful knowledge for the next task.

## Recommended category

**Developer Tools / Engineering** — choose the closest matching production category offered by the submission portal.

## Public URLs

- Website: `https://codelocal.cloud`
- Support: `https://codelocal.cloud/support`
- Privacy: `https://codelocal.cloud/privacy`
- Terms: `https://codelocal.cloud/terms`
- Security: `https://codelocal.cloud/security`
- MCP: `https://codelocal.cloud/mcp`

## Authentication

OAuth. Submit the production server as a **Universal** MCP URL unless OpenAI explicitly approves a template URL.

## Brand notes

- Use the production CodeLocal icon with no beta badge inside the logo itself.
- Keep the product name exactly `CodeLocal` in the listing, OAuth consent surface, website and support/legal pages.
- Position MCP as the protocol underneath CodeLocal, not as the product name.
- Do not claim that CodeLocal stores all source code in Cloud or that Cloud never stores any project knowledge; the privacy copy must match the actual Project Brain architecture.
