{
  "name": "codex-mcp",
  "version": "1.0.0-beta.1",
  "private": true,
  "type": "module",
  "description": "CodeLocal production-beta remote coding bridge with OAuth, pairing, polyglot semantics, sandboxed execution, transactional editing and auditability.",
  "bin": {
    "codelocal": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx watch src/server-v2.ts",
    "build": "tsc --outDir dist",
    "prepare": "npm run build",
    "prestart": "npm run build",
    "start": "node dist/server-v2.js",
    "client": "npm run build --silent && node dist/client-v2.js",
    "doctor": "npm run build --silent && node dist/cli.js doctor",
    "typecheck": "tsc --noEmit",
    "test": "npm run build --silent && node --test dist/__tests__/*.test.js",
    "ci": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "chokidar": "^4.0.3",
    "express": "^5.1.0",
    "ignore": "^7.0.5",
    "typescript": "^5.9.2",
    "ws": "^8.18.3",
    "zod": "^3.25.76"
  },
  "optionalDependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5"
  },
  "engines": { "node": ">=20" }
}
