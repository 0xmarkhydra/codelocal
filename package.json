{
  "name": "codex-mcp",
  "version": "1.5.40",
  "private": true,
  "description": "CodeLocal Go backend/CLI runtime with a Next.js web product.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "go build ./cmd/...",
    "build:web": "npm run build --prefix web",
    "build:all": "npm run build && npm run build:web",
    "typecheck": "go vet ./cmd/... ./internal/...",
    "typecheck:web": "npm run typecheck --prefix web",
    "test": "go test ./cmd/... ./internal/...",
    "check:repo": "node ./scripts/check-repository-structure.mjs",
    "web:install": "npm ci --prefix web",
    "web:dev": "npm run dev --prefix web",
    "web:check": "npm run check --prefix web",
    "verify:web-edge": "node ./scripts/verify-web-edge.mjs",
    "ci:go": "go test ./cmd/... ./internal/... && go vet ./cmd/... ./internal/...",
    "ci:web": "npm ci --prefix web && npm run check --prefix web && npm audit --omit=dev --prefix web",
    "ci": "npm run check:repo && npm run ci:go && npm run ci:web",
    "build:npm": "go run ./cmd/release",
    "release:prepare": "npm run ci && go run ./cmd/release && npm pack --dry-run ./.release/npm",
    "release:npm": "(npm whoami --registry=https://registry.npmjs.org/ >/dev/null 2>&1 || npm login --registry=https://registry.npmjs.org/) && npm whoami --registry=https://registry.npmjs.org/ && bash ./release.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
